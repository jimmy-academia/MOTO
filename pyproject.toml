[project]
name = "anot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "openai>=2.9.0",
    "torch>=2.9.1",
    "trace-opt>=0.1.3.8",
]
